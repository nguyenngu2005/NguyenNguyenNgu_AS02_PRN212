<Window x:Class="NguyenNguyenNguWPF.Views.RoomDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Room" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource HotelPrimaryBrush}">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource HotelAccentBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="DialogTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource HotelTextBoxStyle}">
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="SaveButtonStyle" TargetType="Button" BasedOn="{StaticResource HotelButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource HotelHighlightBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource HotelPrimaryBrush}"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="45"/>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource HotelButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource HotelAccentBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource HotelPrimaryBrush}"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="45"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="260"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Number" Grid.Row="0" Grid.Column="0" Margin="0,8"/>
        <TextBox Text="{Binding RoomNumber}" Grid.Row="0" Grid.Column="1" Margin="0,4" Style="{StaticResource DialogTextBoxStyle}"/>

        <TextBlock Text="Type ID" Grid.Row="1" Grid.Column="0" Margin="0,8"/>
        <TextBox Text="{Binding RoomTypeID}" Grid.Row="1" Grid.Column="1" Margin="0,4" Style="{StaticResource DialogTextBoxStyle}"/>

        <TextBlock Text="Capacity" Grid.Row="2" Grid.Column="0" Margin="0,8"/>
        <TextBox Text="{Binding RoomMaxCapacity}" Grid.Row="2" Grid.Column="1" Margin="0,4" Style="{StaticResource DialogTextBoxStyle}"/>

        <TextBlock Text="Price/Day" Grid.Row="3" Grid.Column="0" Margin="0,8"/>
        <TextBox Text="{Binding RoomPricePerDay}" Grid.Row="3" Grid.Column="1" Margin="0,4" Style="{StaticResource DialogTextBoxStyle}"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save" IsDefault="True" Click="Save_Click" Style="{StaticResource SaveButtonStyle}"/>
            <Button Content="Cancel" Margin="8,0,0,0" IsCancel="True" Style="{StaticResource CancelButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>